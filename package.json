{
  "name": "raggy",
  "version": "1.0.0",
  "description": "A lightweight, local-first RAG server that uses embeddings from a local Ollama instance. It ingests text/markdown/code/PDF files from your filesystem, stores chunk embeddings on disk, and exposes a simple UI and HTTP API for search.",
  "type": "module",
  "main": "dist/index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "start": "node dist/index.js",
    "test": "node --test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^17.2.2",
    "express": "^5.1.0",
    "node-fetch": "^3.3.2",
    "readline": "^1.3.0"
  }
}
